#include<iostream>
using namespace std;
int main()
{
    int n;
    float f;
    char ch;
    double d;
    // double   *p;
    cout<<"size of integer is "<<sizeof(n);
    cout<<endl<<"Size of Char is "<<sizeof(ch);
    cout<<endl<<"size of float is "<<sizeof(f);
    cout<<endl<<"size of double is "<<sizeof(d);
    // cout<<endl<<"size of pointer is "<<sizeof(p);
}